@model student_management.Models.Student

@{
    ViewData["Title"] = "Chi tiết sinh viên";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var age = DateTime.Now.Year - Model.DateOfBirth.Year;
    if (DateTime.Now.DayOfYear < Model.DateOfBirth.DayOfYear) age--;
}

<div class="container">
    <!-- Header Section -->
    <div class="row mb-4">
        <div class="col-md-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-action="Index"><i class="bi bi-house-door"></i> Danh sách sinh viên</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Chi tiết</li>
                </ol>
            </nav>
            <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
                <div>
                    <h1 class="display-6 fw-bold text-primary mb-1">
                        <i class="bi bi-person-badge"></i> Thông tin sinh viên
                    </h1>
                    <p class="text-muted mb-0">
                        <i class="bi bi-info-circle"></i> Xem chi tiết thông tin sinh viên
                    </p>
                </div>
                <div class="d-flex gap-2 flex-wrap">
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">
                        <i class="bi bi-pencil"></i>
                        <span class="d-none d-md-inline ms-1">Chỉnh sửa</span>
                    </a>
                    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger">
                        <i class="bi bi-trash"></i>
                        <span class="d-none d-md-inline ms-1">Xóa</span>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Student Details Card -->
    <div class="row">
        <!-- Left Column - Avatar and Quick Actions -->
        <div class="col-lg-4 mb-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body text-center p-4">
                    <!-- Avatar -->
                    <div class="mb-4 mt-3">
                        @if (!string.IsNullOrEmpty(Model.ImgUrl))
                        {
                            <img src="@Model.ImgUrl" alt="@Model.Name" 
                                 class="student-detail-avatar rounded-circle shadow" 
                                 onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                                 loading="lazy">
                            <div class="student-detail-avatar-fallback rounded-circle shadow mx-auto" style="display:none;">
                                <span class="display-3">@(Model.Name?.FirstOrDefault().ToString().ToUpper() ?? "?")</span>
                            </div>
                        }
                        else
                        {
                            <div class="student-detail-avatar-fallback rounded-circle shadow mx-auto">
                                <span class="display-3">@(Model.Name?.FirstOrDefault().ToString().ToUpper() ?? "?")</span>
                            </div>
                        }
                    </div>
                    
                    <!-- Student Name -->
                    <h3 class="fw-bold mb-2">@Model.Name</h3>
                    
                    <!-- Gender Badge -->
                    <div class="mb-3">
                        @if (Model.Gender == 1)
                        {
                            <span class="badge bg-primary-subtle text-primary px-3 py-2 fs-6">
                                <i class="bi bi-gender-male"></i> Nam
                            </span>
                        }
                        else
                        {
                            <span class="badge bg-danger-subtle text-danger px-3 py-2 fs-6">
                                <i class="bi bi-gender-female"></i> Nữ
                            </span>
                        }
                    </div>
                    
                    <!-- Age Info -->
                    <div class="mb-4 p-3 bg-light rounded">
                        <div class="text-muted small mb-1">Tuổi</div>
                        <div class="fs-4 fw-bold text-primary">@age</div>
                        <div class="text-muted small">tuổi</div>
                    </div>

                    <hr class="my-4">

                    <!-- Quick Actions -->
                    <div class="d-grid gap-2">
                        <a href="mailto:@Model.Email" class="btn btn-outline-primary">
                            <i class="bi bi-envelope"></i> Gửi email
                        </a>
                        <a href="tel:@Model.PhoneNumber" class="btn btn-outline-primary">
                            <i class="bi bi-telephone"></i> Gọi điện
                        </a>
                    </div>

                    <hr class="my-4">

                    <!-- Navigation Buttons -->
                    <div class="d-grid gap-2">
                        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning btn-lg">
                            <i class="bi bi-pencil"></i> Chỉnh sửa
                        </a>
                        <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                            <i class="bi bi-arrow-left"></i> Quay lại danh sách
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Right Column - Detailed Information -->
        <div class="col-lg-8">
            <!-- Personal Information Card -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-person-lines-fill"></i> Thông tin cá nhân
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row g-4">
                        <div class="col-md-12">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-person text-primary me-2"></i>
                                    @Html.DisplayNameFor(model => model.Name)
                                </div>
                                <div class="detail-value">
                                    @Html.DisplayFor(model => model.Name)
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-calendar-event text-primary me-2"></i>
                                    @Html.DisplayNameFor(model => model.DateOfBirth)
                                </div>
                                <div class="detail-value">
                                    @Model.DateOfBirth.ToString("dd/MM/yyyy")
                                    <div class="text-muted small mt-1">
                                        (@Model.DateOfBirth.ToString("dddd, dd MMMM yyyy", new System.Globalization.CultureInfo("vi-VN")))
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-gender-ambiguous text-primary me-2"></i>
                                    @Html.DisplayNameFor(model => model.Gender)
                                </div>
                                <div class="detail-value">
                                    @if (Model.Gender == 1)
                                    {
                                        <span><i class="bi bi-gender-male text-primary me-1"></i> Nam</span>
                                    }
                                    else
                                    {
                                        <span><i class="bi bi-gender-female text-danger me-1"></i> Nữ</span>
                                    }
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-geo-alt text-primary me-2"></i>
                                    @Html.DisplayNameFor(model => model.Region)
                                </div>
                                <div class="detail-value">
                                    <i class="bi bi-pin-map-fill text-primary me-2"></i>
                                    @Html.DisplayFor(model => model.Region)
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-calculator text-primary me-2"></i>
                                    Tuổi hiện tại
                                </div>
                                <div class="detail-value">
                                    @age tuổi
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Contact Information Card -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-telephone-forward"></i> Thông tin liên hệ
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row g-4">
                        <div class="col-md-12">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-envelope text-success me-2"></i>
                                    @Html.DisplayNameFor(model => model.Email)
                                </div>
                                <div class="detail-value">
                                    <a href="mailto:@Model.Email" class="text-decoration-none">
                                        <i class="bi bi-envelope-fill me-2"></i>
                                        @Html.DisplayFor(model => model.Email)
                                    </a>
                                    <button class="btn btn-sm btn-outline-secondary ms-2" onclick="copyToClipboard('@Model.Email')" title="Sao chép">
                                        <i class="bi bi-clipboard"></i>
                                    </button>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-12">
                            <div class="detail-item">
                                <div class="detail-label">
                                    <i class="bi bi-telephone text-success me-2"></i>
                                    @Html.DisplayNameFor(model => model.PhoneNumber)
                                </div>
                                <div class="detail-value">
                                    <a href="tel:@Model.PhoneNumber" class="text-decoration-none">
                                        <i class="bi bi-phone-fill me-2"></i>
                                        @Html.DisplayFor(model => model.PhoneNumber)
                                    </a>
                                    <button class="btn btn-sm btn-outline-secondary ms-2" onclick="copyToClipboard('@Model.PhoneNumber')" title="Sao chép">
                                        <i class="bi bi-clipboard"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Image Information Card -->
            <div class="card shadow-sm border-0">
                <div class="card-header bg-light">
                    <h5 class="mb-0">
                        <i class="bi bi-image"></i> Ảnh đại diện
                    </h5>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(Model.ImgUrl))
                    {
                        <div class="detail-item">
                            <div class="detail-label">
                                <i class="bi bi-link-45deg text-primary me-2"></i>
                                URL ảnh
                            </div>
                            <div class="detail-value text-break">
                                <a href="@Model.ImgUrl" target="_blank" class="text-decoration-none" rel="noopener noreferrer">
                                    @Model.ImgUrl
                                    <i class="bi bi-box-arrow-up-right ms-1"></i>
                                </a>
                                <button class="btn btn-sm btn-outline-secondary ms-2" onclick="copyToClipboard('@Model.ImgUrl')" title="Sao chép URL">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-info mb-0">
                            <i class="bi bi-info-circle me-2"></i>
                            Sinh viên chưa có ảnh đại diện. Đang sử dụng avatar mặc định.
                        </div>
                    }
                </div>
            </div>

            <!-- Metadata Card -->
            <div class="card shadow-sm border-0 mt-4 bg-light">
                <div class="card-body">
                    <div class="row text-center">
                        <div class="col-md-4">
                            <div class="text-muted small mb-1">Mã sinh viên</div>
                            <div class="fw-bold">#@Model.Id</div>
                        </div>
                        <div class="col-md-4">
                            <div class="text-muted small mb-1">Trạng thái</div>
                            <div class="fw-bold text-success">
                                <i class="bi bi-check-circle-fill"></i> Hoạt động
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="text-muted small mb-1">Cập nhật</div>
                            <div class="fw-bold">@DateTime.Now.ToString("dd/MM/yyyy")</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Toast for copy notification -->
<div class="toast-container position-fixed bottom-0 end-0 p-3">
    <div id="copyToast" class="toast align-items-center text-white bg-success border-0" role="alert">
        <div class="d-flex">
            <div class="toast-body">
                <i class="bi bi-check-circle-fill me-2"></i>
                Đã sao chép vào clipboard!
            </div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(function() {
                // Show toast notification
                var toastEl = document.getElementById('copyToast');
                var toast = new bootstrap.Toast(toastEl);
                toast.show();
            }).catch(function(err) {
                alert('Không thể sao chép: ' + err);
            });
        }

        $(document).ready(function() {
            // Add animation to detail items
            $('.detail-item').each(function(index) {
                $(this).css('animation-delay', (index * 0.05) + 's');
            });

            // Add hover effect
            $('.detail-item').hover(
                function() {
                    $(this).addClass('shadow-sm');
                },
                function() {
                    $(this).removeClass('shadow-sm');
                }
            );
        });
    </script>
}
